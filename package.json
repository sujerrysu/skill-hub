{
  "name": "skill-hub",
  "version": "1.0.0",
  "description": "OpenClaw & MCP Skills 收集与展示平台",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "crawler": "node scripts/crawler.js",
    "update": "npm run crawler && git add data && git diff --cached --quiet || git commit -m 'Auto update skills data' && git push"
  },
  "keywords": ["openclaw", "mcp", "skills", "plugins"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  }
}
